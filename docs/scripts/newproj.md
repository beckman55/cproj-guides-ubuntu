# newproj Script

`newproj` creates a new project locally and on GitHub with zero hard-coded values.

## Features

- Auto-detects GitHub username
- Creates a local project directory
- Initializes a Git repository
- Creates a GitHub repository
- Pushes the initial commit
- Ensures reproducible project creation

## Usage

```bash
newproj <project-name>
```
